const maxStrings = (a) => [...a].sort().pop();

const countries = ["Argentina", "Uruguay", "Brasil", "Paraguay"];
console.log(maxStrings(countries));
console.log(countries); // What's the output? Why?